# 后续十项执行任务

## 执行原则

- 按任务依赖连续推进，不在每个小改动后等待确认。
- 只在需要真实数据授权、文件来源选择或改变“不做实盘交易”的边界时暂停。
- 每个任务完成后更新本文件、`.codex-harness/STATE.md` 并提交独立 Git commit。
- 所有回测均固定 `data_version`；fixture 只用于测试与界面演示，不能进入因子库正式批准状态。

| 序号 | 任务 | 主要交付 | 前置条件 | 当前状态 |
| --- | --- | --- | --- | --- |
| 1 | 因子库持久化 | SQLite 因子版本、候选状态、追加式人工决策与单元测试 | 无 | 已完成 |
| 2 | 因子库 API | 从已完成研究运行登记候选、查询列表、人工批准/拒绝/废弃 API | 任务 1 | 已完成 |
| 3 | 因子库工作台 | 网页查看候选、公式、OOS/稳定性指标、来源和数据版本；提交人工决策 | 任务 2 | 已完成 |
| 4 | 因子变形引擎 | 在白名单 DSL 内生成参数、窗口、方向和组合变体；记录父因子与变形理由 | 任务 1 | 已完成 |
| 5 | 研究 Agent 实验编排 | 对候选及变体按固定数据版本执行有限预算回测，保存每轮实验与淘汰原因 | 任务 4 | 已完成 |
| 6 | 基金经理 Agent 建议 | 根据证据、OOS、walk-forward、成本与相似度给出“建议批准/拒绝/继续研究”；最终仍由人工决定 | 任务 2、5 | 已完成 |
| 7 | 原始数据质量扩展 | 增加证券主表、交易日历、停牌/退市/公司行为数据契约和更细质量门禁 | 无 | 已完成 |
| 8 | 两标的一年真实演练 | 导入授权 CSV，发布数据版本，以同一版本两次复跑研究并登记候选 | 任务 7；真实授权 CSV | 已完成（本地授权演练，未产生候选） |
| 9 | 全 A 股 8-10 年基线 | 可恢复回填、发布验收、DBeaver 审计、每日增量子版本 | 任务 8；正式数据快照 | 导入链路已就绪，等待正式快照 |
| 10 | 全流程闭环验收 | 研报上传 -> 提取 -> 裂变 -> 固定版本回测 -> PM 建议 -> 人工因子库决策 -> 反馈下一轮 | 任务 3、6、9 | 待开始 |

## 任务 1-3：因子库基础

因子库不是简单地保存因子名称。每个版本必须保存 DSL 公式、字段、方向、原始证据、指标、研究 `run_id`、数据 `data_version` 和 manifest 哈希。只有人工批准记录存在时状态才是 `approved`；候选状态不代表可用因子。

工作台可以从当前或历史研究运行登记已选因子，并在页面内追加人工决策。基金经理 Agent 的建议会单独记录，不能更改因子库状态。

## 任务 4-6：多 Agent 研究闭环

变形引擎不能任意生成 Python 或无限调用 LLM。它只能在受限 DSL 和任务预算内工作。研究 Agent 只负责实验和淘汰，基金经理 Agent 只负责建议；二者都不能绕过人工审批或固定数据版本。

实验 API 仅接受使用 `warehouse` 且绑定已发布 `data_version` 的已完成研究运行。实验记录候选上限、变形轮数、回测上限、零 LLM 调用、父因子与每个淘汰原因。

## 任务 7-9：可信数据底座

真实日频数据在系统中优先级高于研究结果。任务 8 与任务 9 必须使用可授权、可复核的本地快照，不能用 adata、AKShare 或 fixture 伪装为正式历史库。任务 8 是全市场回填前唯一必要演练。

发布前质量检查现覆盖可选的证券主表、交易日历、证券状态和公司行为契约；一旦提供，格式或主从一致性错误会阻止发布。缺少这些参考表仍会保留警示，以兼容当前受控 bars-only CSV 演练入口。

本地两标的一年演练记录见 [验收记录](market-data-two-symbol-rehearsal.md)。该快照仅验证流程和版本复现，不能替代任务 9 所需的正式全市场离线基线。

## 任务 10：验收标准

从一份上传研报开始，系统应能产生可解释因子候选、记录变形谱系、使用一个已发布的数据版本回测、展示 OOS 与稳定性门禁、给出基金经理 Agent 建议，并由人工在因子库中留下最终决定。整个链条无需交易账户、券商连接或下单模块。
